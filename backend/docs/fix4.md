请根据以下问题生成一个清晰、可执行的提示词，用于指导Claude Code诊断和解决前端登录请求返回502错误的问题。提示词应包含问题背景、怀疑原因、检查方向和具体任务，以便Claude能够准确理解并执行分析。

---

**问题描述**  
服务启动成功，但登录页面点击登录时，浏览器控制台报错：  
`index-s1jDr8fo.js:27 POST http://127.0.0.1:8089/api/auth/login 502 (Bad Gateway)`  
该错误仅在使用了代码混淆后出现；未使用混淆时，登录功能正常。怀疑是混淆代码导致的问题，并担心混淆可能对其他组件产生潜在错误。

**要求**  
请你（Claude Code）扮演调试专家，帮助开发者全面检查混淆代码可能引发的问题，并给出诊断步骤和解决方案。请按照以下任务清单执行分析：

1. **检查混淆配置**  
   - 查看项目中使用的混淆工具（如JavaScript混淆器）及其配置文件（如`webpack.config.js`、`terser`选项、`javascript-obfuscator`配置等）。  
   - 识别混淆规则中是否启用了对字符串、变量名、函数名、属性名的修改，以及是否启用了控制流平坦化、死代码注入等高级选项。

2. **对比混淆前后的代码**  
   - 选取登录功能相关的核心文件（如处理登录请求的模块、API调用封装文件），对比混淆前后的代码差异。  
   - 重点关注API请求的URL、HTTP方法、请求头、请求体构造部分的代码是否被混淆改变。

3. **分析网络请求**  
   - 模拟登录操作，使用浏览器开发者工具查看混淆后发出的实际网络请求：  
     - 请求URL是否正确（仍为`http://127.0.0.1:8089/api/auth/login`）？  
     - 请求方法是否为`POST`？  
     - 请求头（如`Content-Type`、自定义头）是否被意外修改或丢失？  
     - 请求体格式（JSON/FormData）是否正确？  
   - 如果请求URL或参数发生变化，定位到修改这些值的混淆代码段。

4. **检查后端服务连通性**  
   - 确认后端服务是否仍在`127.0.0.1:8089`正常运行（使用`curl`或Postman直接调用登录接口测试）。  
   - 观察后端日志，看是否有请求到达以及处理状态。502可能表示网关超时或后端未响应，需排除后端本身问题。

5. **排查混淆对运行时代码的影响**  
   - 检查混淆后代码中是否存在语法错误、未定义的变量、被意外覆盖的全局对象等，这些可能导致前端请求无法正确发出。  
   - 在浏览器中执行混淆后的登录相关代码，查看是否有其他控制台错误（如`Uncaught TypeError`）阻止了请求发送。

6. **验证其他组件是否受影响**  
   - 遍历应用的主要功能（如页面跳转、数据获取、表单提交），确认是否出现类似请求失败或界面异常。  
   - 特别关注那些与登录共享相同API客户端或请求库的模块。

7. **提供修复建议**  
   - 根据发现的问题，建议调整混淆配置（例如禁用对API字符串的混淆、保留特定变量名）。  
   - 如果混淆工具本身存在bug，考虑升级版本或更换混淆方案。  
   - 给出测试混淆后功能的步骤，确保所有核心流程正常。

请按以上要点逐步分析，并将诊断过程和结论清晰输出，附带可操作的修复建议。
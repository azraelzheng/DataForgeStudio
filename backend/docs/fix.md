1. 许可证信息已使用的用户、报表、数据源与实际数据不一致。
2. 用户安装完成后应该自动启动服务，不需要用户打开系统管理工具再启动服务。
3. 安装后的“C:\Program Files\DataForgeStudio\tools\scripts”现在是不是可以取消打包，因为已经在安装过程安装了服务。
4. C:\Program Files\DataForgeStudio\tools\nssm 里面的文件或者文件夹是否可以直接放在webserver中方便管理，或者你可以建议应该存放的文件夹。
5. 权限组根据之前的说明应该会预置3个组别
6. 用户管理根据之前的说明应该预置用户“admin”，密码“Admin@123”
7. 数据源管理，选择已经存在的数据源点击“编辑”按钮后弹出的编辑数据源界面，应该显示所有记录的信息，包括：数据源名称、数据库类型、服务器地址、端口、用户名、密码、数据库名、描述。
8. 报表设计，选择已经保存的报表点击“编辑”按钮后弹出的界面，应该显示所有记录的信息，包括：报表名称、分类、描述、数据源、sql语句、字段配置、查询条件、视图配置这些信息。
9. 点击新增报表时不应该显示之前输入的数据。
10. 首页显示的仪表数据与系统实际数据不一致。
11. 备份管理，“创建备份”按钮左侧的路径输入框。我需要效果是：点击“创建备份”按钮后弹出树状文件夹选择窗口，可以选择当前电脑的所有分区中的文件夹作为备份文件存放路径，选中文件夹后点击选择窗口的“确定”按钮进行备份。
12. 备份管理—备份计划，新增计划中的备份路径输入框右边的说明改为选择按钮，点击后弹出树状文件夹选择窗口，可以选择当前电脑的所有分区中的文件夹作为备份文件存放路径，选中文件夹后点击选择窗口的“确定”按钮，将选择中的文件夹路径写入到左侧的备份路径输入框。
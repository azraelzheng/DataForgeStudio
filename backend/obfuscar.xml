<?xml version='1.0'?>
<Obfuscator>
  <!-- 输入输出路径配置 -->
  <Var name="InPath" value="src" />
  <Var name="OutPath" value="Obfuscated" />

  <!-- 混淆设置 -->
  <Var name="KeepPublicApi" value="false" />
  <Var name="HidePrivateApi" value="true" />
  <Var name="RenameProperties" value="true" />
  <Var name="RenameEvents" value="true" />
  <Var name="RenameFields" value="true" />
  <Var name="UseUnicodeNames" value="true" />
  <Var name="HideStrings" value="true" />
  <Var name="OptimizeMethods" value="true" />
  <Var name="SuppressIldasm" value="true" />

  <!-- DataForgeStudio.Core.dll 混淆配置 -->
  <Module file="$(InPath)/DataForgeStudio.Core/bin/Release/net8.0/DataForgeStudio.Core.dll">
    <!-- 跳过所有接口 (I*Service, I*Repository 等) -->
    <SkipType name="DataForgeStudio.Core.Interfaces.*" />

    <!-- 跳过服务实现类 (API 层通过类名注册依赖注入) -->
    <SkipType name="DataForgeStudio.Core.Services.*" />

    <!-- 跳过配置类 (需要被 API 层访问) -->
    <SkipType name="DataForgeStudio.Core.Configuration.*" />
  </Module>

  <!-- DataForgeStudio.Shared.dll 混淆配置 -->
  <Module file="$(InPath)/DataForgeStudio.Shared/bin/Release/net8.0/DataForgeStudio.Shared.dll">
    <!-- 跳过所有 DTO 类 (API 响应/请求模型) -->
    <SkipType name="DataForgeStudio.Shared.DTO.*" />

    <!-- 跳过异常类 (需要保留异常消息) -->
    <SkipType name="DataForgeStudio.Shared.Exceptions.*" />

    <!-- 跳过常量类 (公共访问需要) -->
    <SkipType name="DataForgeStudio.Shared.Constants.*" />
  </Module>

  <!-- 注意：DataForgeStudio.Domain 和 DataForgeStudio.Api 不参与混淆 -->
  <!-- Domain 包含 EF Core 实体，混淆会导致映射失败 -->
  <!-- Api 是入口程序集，需要保持公共 API -->

</Obfuscator>
